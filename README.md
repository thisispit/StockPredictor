# python-test

Repository generated by ChatGPT — contains code to fetch NSE OCLHV data (INFY, TCS, NIFTY IT), compute features, visualize with Bokeh, and run quick sklearn models with pytest tests.

**How to use (summary)**
1. Install dependencies: `pip install -r requirements.txt`
2. Fetch data: `python data_fetch.py --tickers INFY TCS --index NIFTYIT --start 2015-01-01 --end 2016-12-31`
3. Create features: `python features.py --input-dir data --output-dir processed`
4. Visualize: `python visualize_bokeh.py --input processed/combined.csv --outplots plots`
5. Run models & tests: `pytest -q`
6. Quick prediction CLI: `python stockpredictor.py INFY`

See individual files for more details.
